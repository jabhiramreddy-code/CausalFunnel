# Frontend environment variables
# Copy to .env and fill in your deployed backend URL

# Your Render backend URL — used in production (Vite proxy handles it in dev)
VITE_API_URL=https://YOUR_RENDER_APP.onrender.com/api
